# boot.py — runs before main.py on the Pico.
#
# Standard MicroPython on RP2040 exposes USB CDC only (no mass storage),
# so no special configuration is needed here. This file is kept as a
# placeholder — delete it or leave it empty.
